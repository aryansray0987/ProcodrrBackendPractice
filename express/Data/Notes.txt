Zod is a schema validation library for JavaScript / TypeScript.
In simple words:
Zod checks whether your data is in the correct shape.
ex. import { z } from "zod";

const UserSchema = z.object({
  name: z.string(),
  age: z.number(),
  email: z.string().email(),
});

